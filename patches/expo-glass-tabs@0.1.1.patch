diff --git a/build/glass-tab-bar.d.ts b/build/glass-tab-bar.d.ts
index 1ff17b735de13c8c7f3dc13fe5af2520405040a6..7ccbde985eaa497c1b6bc499761e64dbfde08147 100644
--- a/build/glass-tab-bar.d.ts
+++ b/build/glass-tab-bar.d.ts
@@ -28,13 +28,17 @@ export type GlassTabBarProps = TabListProps & {
     theme?: Partial<GlassTabBarTheme>;
     /** Haptic tick while the scrub crosses tab boundaries (iOS). */
     haptics?: boolean;
+    /** Native Liquid Glass background when available. */
+    liquidGlass?: boolean;
+    /** Progressive blur rising from the screen edge behind the pill. */
+    progressiveBlur?: boolean;
 };
 /**
  * Floating liquid-glass tab bar with Revolut-style minimize-on-scroll,
  * a sliding highlight, and finger scrubbing. Use via `TabList asChild`
  * with expo-router's headless tabs.
  */
-export declare function GlassTabBar({ children, onIndexSelected, theme: themeOverrides, haptics, ...props }: GlassTabBarProps): import("react").JSX.Element;
+export declare function GlassTabBar({ children, onIndexSelected, theme: themeOverrides, haptics, liquidGlass, progressiveBlur, ...props }: GlassTabBarProps): import("react").JSX.Element;
 /** One tab trigger: icon + label that fades when minimized. */
 export declare function GlassTabButton({ item, index, isFocused, onPress, ...props }: TabTriggerSlotProps & {
     item: GlassTabItem;
diff --git a/build/glass-tab-bar.js b/build/glass-tab-bar.js
index 420cb04242abe5c71811088fd76bb91090558c5f..554658d4ea45bda14c1cd3688a5e3ac88ed1f511 100644
--- a/build/glass-tab-bar.js
+++ b/build/glass-tab-bar.js
@@ -49,7 +49,7 @@ const BarContext = createContext(null);
  * a sliding highlight, and finger scrubbing. Use via `TabList asChild`
  * with expo-router's headless tabs.
  */
-export function GlassTabBar({ children, onIndexSelected, theme: themeOverrides, haptics = true, ...props }) {
+export function GlassTabBar({ children, onIndexSelected, theme: themeOverrides, haptics = true, liquidGlass = true, progressiveBlur = true, ...props }) {
     const insets = useSafeAreaInsets();
     const { width: windowWidth } = useWindowDimensions();
     const minimized = useMinimizeState();
@@ -160,13 +160,13 @@ export function GlassTabBar({ children, onIndexSelected, theme: themeOverrides,
     });
     const bottomOffset = Math.max(insets.bottom - 16, 12);
     const barContext = useMemo(() => ({ slideIndex, isDragging, theme }), [slideIndex, isDragging, theme]);
-    return (_jsxs(View, { ...props, pointerEvents: "box-none", style: { position: 'absolute', left: 0, right: 0, bottom: 0 }, children: [_jsx(ProgressiveBlur, { direction: "bottom", style: {
+    return (_jsxs(View, { ...props, pointerEvents: "box-none", style: { position: 'absolute', left: 0, right: 0, bottom: 0 }, children: [progressiveBlur ? (_jsx(ProgressiveBlur, { direction: "bottom", style: {
                     position: 'absolute',
                     left: 0,
                     right: 0,
                     bottom: 0,
                     height: bottomOffset + EXPANDED_HEIGHT + BLUR_BLEED,
-                } }), _jsx(View, { pointerEvents: "box-none", style: { marginHorizontal: BAR_MARGIN, marginBottom: bottomOffset }, children: _jsx(GestureDetector, { gesture: gesture, children: _jsxs(Animated.View, { style: barStyle, children: [isLiquidGlassAvailable() ? (_jsx(AnimatedGlassView, { glassEffectStyle: "regular", style: [
+                } })) : null, _jsx(View, { pointerEvents: "box-none", style: { marginHorizontal: BAR_MARGIN, marginBottom: bottomOffset }, children: _jsx(GestureDetector, { gesture: gesture, children: _jsxs(Animated.View, { style: barStyle, children: [liquidGlass && isLiquidGlassAvailable() ? (_jsx(AnimatedGlassView, { glassEffectStyle: "regular", style: [
                                     StyleSheet.absoluteFill,
                                     { backgroundColor: theme.glassTint, borderCurve: 'continuous' },
                                     shapeStyle,
diff --git a/src/glass-tab-bar.tsx b/src/glass-tab-bar.tsx
index c0f798ba35d9722d523f0cbe57ea11c8b8773f5c..bedbada1fa8e967d672c31e8baaf34fac88b0d7a 100644
--- a/src/glass-tab-bar.tsx
+++ b/src/glass-tab-bar.tsx
@@ -100,6 +100,10 @@ export type GlassTabBarProps = TabListProps & {
   theme?: Partial<GlassTabBarTheme>;
   /** Haptic tick while the scrub crosses tab boundaries (iOS). */
   haptics?: boolean;
+  /** Native Liquid Glass background when available. */
+  liquidGlass?: boolean;
+  /** Progressive blur rising from the screen edge behind the pill. */
+  progressiveBlur?: boolean;
 };
 
 /**
@@ -112,6 +116,8 @@ export function GlassTabBar({
   onIndexSelected,
   theme: themeOverrides,
   haptics = true,
+  liquidGlass = true,
+  progressiveBlur = true,
   ...props
 }: GlassTabBarProps) {
   const insets = useSafeAreaInsets();
@@ -269,23 +275,24 @@ export function GlassTabBar({
       {...props}
       pointerEvents="box-none"
       style={{ position: 'absolute', left: 0, right: 0, bottom: 0 }}>
-      {/* Progressive blur rising from the screen's bottom edge behind the pill. */}
-      <ProgressiveBlur
-        direction="bottom"
-        style={{
-          position: 'absolute',
-          left: 0,
-          right: 0,
-          bottom: 0,
-          height: bottomOffset + EXPANDED_HEIGHT + BLUR_BLEED,
-        }}
-      />
+      {progressiveBlur ? (
+        <ProgressiveBlur
+          direction="bottom"
+          style={{
+            position: 'absolute',
+            left: 0,
+            right: 0,
+            bottom: 0,
+            height: bottomOffset + EXPANDED_HEIGHT + BLUR_BLEED,
+          }}
+        />
+      ) : null}
       <View
         pointerEvents="box-none"
         style={{ marginHorizontal: BAR_MARGIN, marginBottom: bottomOffset }}>
         <GestureDetector gesture={gesture}>
           <Animated.View style={barStyle}>
-            {isLiquidGlassAvailable() ? (
+            {liquidGlass && isLiquidGlassAvailable() ? (
               <AnimatedGlassView
                 glassEffectStyle="regular"
                 style={[
